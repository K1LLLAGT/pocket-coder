# Privacy notes

This repository is an integration snapshot.

Notes:
- Audit upstream analytics/ad integrations before production release.
- Keep user-provided model/data local by default.
- Never commit secrets (API keys, keystore passwords).
